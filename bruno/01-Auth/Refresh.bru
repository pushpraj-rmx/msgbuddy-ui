meta {
  name: Refresh
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/auth/refresh
  body: json
  auth: none
}

params:query {
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "refreshToken": "{{refreshToken}}"
  }
}
